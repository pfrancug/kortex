{
  "name": "@kortex/demo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "ts": "tsc --noEmit"
  },
  "dependencies": {
    "@kortex/core": "file:../../packages/core"
  }
}
