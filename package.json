{
  "name": "nexgraph",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "build:core": "npm run build --workspace=@nexgraph/core",
    "ci": "npm run ts && npm run lint && npm run test",
    "dev": "npm run dev --workspace=@nexgraph/demo",
    "dev:demo": "npm run dev --workspace=@nexgraph/demo",
    "dev:react-demo": "npm run dev --workspace=@nexgraph/react-demo",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\"",
    "lint": "npm run lint --workspaces --if-present",
    "publish:core": "npm publish --workspace=@nexgraph/core --access public",
    "publish:react": "npm publish --workspace=@nexgraph/react --access public",
    "release:build": "npm run clean --workspace=@nexgraph/core && npm run clean --workspace=@nexgraph/react && npm run build --workspace=@nexgraph/core && npm run build --workspace=@nexgraph/react",
    "test": "npm run test --workspaces --if-present",
    "ts": "npm run ts --workspaces --if-present"
  },
  "devDependencies": {
    "prettier": "3.8.3",
    "rimraf": "6.1.3",
    "typescript": "6.0.3",
    "vite": "8.0.11"
  }
}
